cmake_minimum_required(VERSION 3.12)
project( cudaof )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( cudaof cudaof.cpp )
target_link_libraries( cudaof ${OpenCV_LIBS} )
